description: |
  Positions a element with `position: absolute;` inside the bounding box.
mixin:
  parameters:
    width: "element width"
    height: "element height"
    valign: "(optional, default=top) vertical alignment top|middle|bottom"
    halign: "(optional, default=left) horizontal alignment left|center|right"
    xOffset: "(optional, default=0) x position offset"
    yOffset: "(optional, default=0) y position offset"
  less: |
    ._cssowl-position-absolute-inside-vars(@width, @height, @valign:top, @halign:left, @xOffset:0, @yOffset:0) when (@valign = top) {
      @top: @yOffset;
      @bottom: none;
    }
    ._cssowl-position-absolute-inside-vars(@width, @height, @valign:top, @halign:left, @xOffset:0, @yOffset:0) when (@valign = middle) {
      @top: 50%;
      margin-top: @yOffset - round(@height / 2);
      @bottom: none;
    }
    ._cssowl-position-absolute-inside-vars(@width, @height, @valign:top, @halign:left, @xOffset:0, @yOffset:0) when (@valign = bottom) {
      @top: none;
      @bottom: @xOffset;
    }
    ._cssowl-position-absolute-inside-vars(@width, @height, @valign:top, @halign:left, @xOffset:0, @yOffset:0) when (@halign = left) {
      @left: @xOffset;
      @right: none;
    }
    ._cssowl-position-absolute-inside-vars(@width, @height, @valign:top, @halign:left, @xOffset:0, @yOffset:0) when (@halign = center) {
      @left: 50%;
      margin-left: @xOffset - round(@width / 2);
      @right: none;
    }
    ._cssowl-position-absolute-inside-vars(@width, @height, @valign:top, @halign:left, @xOffset:0, @yOffset:0) when (@halign = right) {
      @left: none;
      @right: @xOffset;
    }
    ._cssowl-position-absolute-inside-vars(@width, @height, @valign:top, @halign:left, @xOffset:0, @yOffset:0) {
    }
    .cssowl-position-absolute-inside(@width, @height, @valign:top, @halign:left, @xOffset:0, @yOffset:0) {
      ._cssowl-position-absolute-inside-vars(@width, @height, @valign, @halign, @xOffset, @yOffset);
      .cssowl-position-absolute(@top, @right, @bottom, @left);
    }
  sass: |
    @mixin cssowl-position-absolute-inside($width, $height, $valign:top, $halign:left, $xOffset:0, $yOffset:0)
      $top: none
      $left: none
      $right: none
      $bottom: none
      @if $valign == top
        $top: $yOffset
      @else if $valign == middle
        $top: 50%
        margin-top: $yOffset - round($height / 2)
      @else if $valign == bottom
        $bottom: $yOffset
      @if $halign == left
        $left: $xOffset
      @else if $halign == center
        $left: 50%
        margin-left: $xOffset - round($width / 2)
      @else if $halign == right
        $right: $xOffset
      +cssowl-position-absolute($top, $right, $bottom, $left)
  scss: |
    @mixin cssowl-position-absolute-inside($width, $height, $valign:top, $halign:left, $xOffset:0, $yOffset:0) {
      $top:none;
      $left:none;
      $right:none;
      $bottom:none;
      @if $valign == top {
        $top: $yOffset;
      }
      @else if $valign == middle {
        $top: 50%;
        margin-top: $yOffset - round($height / 2);
      }
      @else if $valign == bottom {
        $bottom: $yOffset;
      }
      @if $halign == left {
        $left: $xOffset;
      }
      @else if $halign == center {
        $left: 50%;
        margin-left: $xOffset - round($width / 2);
      }
      @else if $halign == right {
        $right: $xOffset;
      }
      @include cssowl-position-absolute($top, $right, $bottom, $left);
    }
  styl: |
    cssowl-position-absolute-inside(width, height, valign=top, halign=left, xOffset=0, yOffset=0)
      if valign == top
        top = yOffset
        bottom = none
      else if valign == middle
        top = 50%
        margin-top: yOffset - round(height / 2)
        bottom = none
      else if valign == bottom
        top = none
        bottom = yOffset
      if halign == left
        left = xOffset
        right = none
      else if halign == center
        left = 50%
        margin-left: xOffset - round(width / 2)
        right = none
      else if halign == right
        left = none
        right = xOffset
      cssowl-position-absolute(top, right, bottom, left)
examples:
  sass: |
    .example-absolute-inside
      width: 10px
      height: 10px
      background: #59371f
      &.top-center
        +cssowl-position-absolute-inside(10px, 10px, top, center)
      &.middle-right
        +cssowl-position-absolute-inside(10px, 10px, middle, right)
      &.bottom-center
        +cssowl-position-absolute-inside(10px, 10px, bottom, center)
      &.middle-left
        +cssowl-position-absolute-inside(10px, 10px, middle, left)
  scss: |
    .example-absolute-inside {
      width: 10px
      height: 10px
      background: #59371f
      &.top-center
        @include cssowl-position-absolute-inside(10px, 10px, top, center);
      }
      &.middle-right {
        @include cssowl-position-absolute-inside(10px, 10px, middle, right);
      }
      &.bottom-center {
        @include cssowl-position-absolute-inside(10px, 10px, bottom, center);
      }
      &.middle-left {
        @include cssowl-position-absolute-inside(10px, 10px, middle, left);
      }
    }
  less: |
    .example-absolute-inside {
      width: 10px;
      height: 10px;
      background: #59371f;
      &.top-center
        .cssowl-position-absolute-inside(10px, 10px, top, center);
      }
      &.middle-right {
        .cssowl-position-absolute-inside(10px, 10px, middle, right);
      }
      &.bottom-center {
        .cssowl-position-absolute-inside(10px, 10px, bottom, center);
      }
      &.middle-left {
        .cssowl-position-absolute-inside(10px, 10px, middle, left);
      }
    }
  stylus: |
    .example-absolute-inside
      width: 10px
      height: 10px
      background: #59371f
      &.top-center
        cssowl-position-absolute-inside(10px, 10px, top, center)
      &.middle-right
        cssowl-position-absolute-inside(10px, 10px, middle, right)
      &.bottom-center
        cssowl-position-absolute-inside(10px, 10px, bottom, center)
      &.middle-left
        cssowl-position-absolute-inside(10px, 10px, middle, left)
  html: |
    .wrapper{:style => "position:relative;width:50px;height:50px;border:1px solid #59371f;"}
      .example-absolute-inside.top-center
      .example-absolute-inside.middle-right
      .example-absolute-inside.bottom-center
      .example-absolute-inside.middle-left
